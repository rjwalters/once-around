{
  "name": "once-around",
  "version": "1.0.0",
  "description": "Visualizer for Paul Fellows' Once Around astronomy series",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter once-around-web dev",
    "build": "pnpm --filter once-around-web build",
    "build:wasm": "pnpm --filter once-around-web build:wasm",
    "build:all": "pnpm --filter once-around-web build:all",
    "deploy:preview": "pnpm --filter once-around-web deploy:preview",
    "deploy:prod": "pnpm --filter once-around-web deploy:prod",
    "deploy:all": "pnpm --filter once-around-web deploy:all",
    "preview": "pnpm --filter once-around-web preview",
    "test": "playwright test",
    "test:perf": "playwright test tests/performance.spec.ts --reporter=list",
    "test:ui": "playwright test --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
