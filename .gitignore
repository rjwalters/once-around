# Dependencies
node_modules/

# Build outputs
dist/
target/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Logs
*.log

# WASM build artifacts (keep .gitignore in wasm dir)
apps/web/src/wasm/*.wasm
apps/web/src/wasm/*.js
apps/web/src/wasm/*.ts
apps/web/src/wasm/package.json
!apps/web/src/wasm/.gitignore
test-results/
playwright-report/
tests/screenshots/

# Repo Skills machine-local install metadata (absolute source path + timestamp)
.claude/skills/repo/.install-local.json
# Loom worktree marker (written by worktree.sh into each issue worktree; not in the managed block yet)
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/tokens/
.loom/accounts.env
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/logs/
# <<< loom-managed <<<

# Local coordination database
/.squad/

# Local Worker state and operator-specific deployment configuration
.wrangler/
.dev.vars*
apps/report-worker/wrangler.preview.jsonc
