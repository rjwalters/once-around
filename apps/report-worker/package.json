{
  "name": "once-around-report-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env local --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply REPORTS --env local --local",
    "migrate:production": "wrangler d1 migrations apply REPORTS --env production --remote",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "wrangler": "4.59.1"
  }
}
