/**
 * Renderer Module (Facade)
 *
 * Re-exports from the renderer/ subdirectory.
 * This preserves the existing import path for consumers.
 */

export { createRenderer, type SkyRenderer } from "./renderer/index";
